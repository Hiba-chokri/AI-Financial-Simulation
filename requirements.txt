# Daba.Dar — RUNTIME dependencies (what the API needs to serve requests).
# This is what the Docker image installs. For local development (tests, Streamlit,
# plots, legacy scraper) install requirements-dev.txt instead — it includes this file.

# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.128.8
uvicorn==0.39.0
pydantic==2.13.4
python-dotenv==1.2.1          # loads .env (API keys, CORS origins) in app/core/config.py

# ── ML inference (loading + running the trained pipeline) ─────────────────────
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
xgboost==2.1.4
joblib==1.5.3
