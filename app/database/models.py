"""
PLACEHOLDER — SQLAlchemy ORM models (not yet implemented).

Planned home for persisted entities (e.g. saved simulations, API-key records,
audit logs) once the service needs a database. Intentionally empty for now; the
live service is stateless and holds no DB dependency at runtime.

See requirements.txt (sqlalchemy, psycopg2-binary) and .env.example (POSTGRES_*).
"""
