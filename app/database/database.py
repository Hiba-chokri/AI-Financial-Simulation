"""
PLACEHOLDER database connection/session setup (not yet implemented).

Planned home for the SQLAlchemy engine + session factory (reading POSTGRES_* from
the environment) once the service needs persistence. Intentionally empty for now;
the live service is stateless. See models.py for the planned ORM entities.
"""
